import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [
    // В данный момент нет официального @vitejs/plugin-angular.
    // Если вы используете Angular, Vite чаще всего используется для Storybook,
    // или как часть более сложной сборки.
    // Если появится официальный плагин или вы используете сторонний,
    // раскомментируйте или замените следующую строку:
    // angular(),
  ],
  resolve: {
    alias: {
      // Здесь можно настроить алиасы для удобства импорта
      // '@app': '/src/app',
    },
  },
  build: {
    // Опции сборки для продакшена
    outDir: 'dist', // Директория для собранного проекта
    emptyOutDir: true, // Очищать outDir перед сборкой
  },
  server: {
    // Опции dev сервера
    port: 4200, // Порт, на котором будет запускаться dev сервер
    open: true, // Открывать браузер автоматически
  },
  optimizeDeps: {
    // Опции для оптимизации зависимостей, чтобы Vite мог их обрабатывать
    // Для Angular может потребоваться указать некоторые зависимости.
    // Например:
    // include: [
    //   '@angular/core',
    //   '@angular/common',
    //   // ...другие Angular модули, которые могут быть проблемными
    // ],
  },
});